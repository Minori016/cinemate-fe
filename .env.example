# Leave empty when the frontend and API share an origin through a reverse proxy.
# For a separate backend deployment, use its origin without a trailing slash:
# VITE_API_URL=https://api.example.com
VITE_API_URL=
