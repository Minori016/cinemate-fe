**Definition of Done** 
*	[x] Cập nhật hoàn chỉnh luồng tạo Lịch Chiếu (Showtime) từ Frontend xuống Backend.
*	[x] Xóa bỏ hoàn toàn Mock Data ở Frontend, sử dụng dữ liệu thực từ API Database.
*	[x] Đồng bộ giá trị sức chứa (capacity) của phòng chiếu đúng với sơ đồ ghế (Seat Layout).
*	[x] Sửa lỗi giao diện bị lệch cột (CSS Flexbox) trên bảng danh sách lịch chiếu.
*	[x] Fix lỗi Exception 400 và chuẩn hóa log lỗi ở Backend.
--- 
**Review Checklist** 
*	[x] PR is linked to a Jira/Trello task or issue 
*	[x] PR title follows naming convention ([Feature], [Fix], [Refactor], etc.) 
*	[x] Không còn mã nguồn thừa (console.log, mock data cứng).
*	[x] Các API liên quan đến Showtime được handle đúng chuẩn REST.
*	[x] Tested kịch bản tự động tạo (Auto-generate) và tạo thủ công (Manual) thành công.
*	[x] Xử lý đúng mapping DTO giữa Entity và JSON response.
--- 
**Table of Contents (Thay đổi chính)**
1. **Frontend (`cinemate-fe`)**
    *   `showtimeService.js`: Gỡ mock data, map DTO `ShowtimeResponse` từ API.
    *   `ShowtimeListPage.jsx`: Fix CSS lệch cột (`flex` trên `td`), xử lý logic load data từ Backend.
    *   `CinemaRoomListPage.jsx` / `CinemaRoomDetailPage.jsx`: Sửa logic hiển thị capacity.
2. **Backend (`cinemate`)**
    *   `AdminShowtimeController.java`: Thêm API `GET /api/v1/admin/showtimes`.
    *   `ShowtimeResponse.java`: Bổ sung `movieTitle`, `roomId`, `basePrice`, v.v...
    *   `CinemaRoomService.java`: Fix lỗi đếm số lượng ghế (Ghế đôi = 2) khi tính `capacity`.
    *   `GlobalExceptionHandler.java`: Bắt lỗi validation `400 Bad Request`.
